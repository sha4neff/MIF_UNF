
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КонтекстОперации = Неопределено;
	Если Параметры.Свойство("КонтекстОперации", КонтекстОперации) Тогда
		Для каждого Ошибка Из КонтекстОперации.Диагностика.Ошибки Цикл
			НоваяСтрока = Ошибки.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, Ошибка);
		КонецЦикла; 
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыОшибки

&НаКлиенте
Процедура ОшибкиВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ЗначениеДляОтображения = "";
	Если Поле = Элементы.ОшибкиВидОперации Тогда
		ЗначениеДляОтображения = Элемент.ТекущиеДанные.ВидОперации;
	ИначеЕсли Поле = Элементы.ОшибкиКраткоеПредставлениеОшибки Тогда
		ЗначениеДляОтображения = Элемент.ТекущиеДанные.КраткоеПредставлениеОшибки;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЗначениеДляОтображения) Тогда
		ПоказатьЗначение(, ЗначениеДляОтображения);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЖурналРегистрации(Команда)
	
	ЭлектронноеВзаимодействиеОбработкаОшибокКлиент.ОткрытьЖурналРегистрации(КонтекстОперации, Неопределено);
	
КонецПроцедуры

#КонецОбласти
