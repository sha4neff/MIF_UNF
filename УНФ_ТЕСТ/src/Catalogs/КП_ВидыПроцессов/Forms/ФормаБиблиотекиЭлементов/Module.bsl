&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	МакетСхемы=Справочники.КП_ВидыПроцессов.ПолучитьМакет("СхемаБиблиотекиЭлементов");
	ИмяВременногоФайла=ПолучитьИмяВременногоФайла(".grp");
	МакетСхемы.Записать(ИмяВременногоФайла);
	
	СхемаБиблиотеки.Прочитать(ИмяВременногоФайла);

КонецПроцедуры
