#Область ОбработчикиСобытий

Процедура ПриЗаписи(Отказ)
	Если ПометкаУдаления Тогда
		//пометим на удаление и объект плана видов характеристик
		
		Если ЗначениеЗаполнено(СвязьСПВХ) Тогда
			ОбъектПВХ=СвязьСПВХ.ПолучитьОбъект();
			Попытка
				ОбъектПВХ.УстановитьПометкуУдаления(Истина);
			Исключение
				ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ОписаниеОшибки());
				Возврат;
			КонецПопытки;

		КонецЕсли;
		
	КонецЕсли;
КонецПроцедуры

#КонецОбласти